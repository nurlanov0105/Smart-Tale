import { CardsSection } from "@/widgets/user/cardsSection";
import { FC } from "react";

const EquipmentPage: FC = () => {
   return <CardsSection isLoading={true} isError={false} />;
};

export default EquipmentPage;
