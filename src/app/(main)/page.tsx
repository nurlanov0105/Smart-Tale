import { Header } from "@/widgets/header";
import Navbar from "@/widgets/navbar/ui/Navbar";

const Home = () => {
   return (
      <div>
         <div>Home</div>
         <Header />
      </div>
   );
};

export default Home;
