export type HeadingValuesTypes = {
    [key: string]: {
        title: string,
        titleLogin?: string,
        subtitle: string,
        loading?: string,
        error?: string
    }
}



export type HeadingTypes = {
    isLoading: boolean
    isError: boolean
}

