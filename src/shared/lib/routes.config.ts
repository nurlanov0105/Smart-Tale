export enum DASHBOARD {
   HOME = "/marketplace/equipment",
   SEARCH = `/search`,
   PROFILE = `/profile/=id`,
}

export enum ROUTES {
   HOME = "/",
   SIGN_UP = "/auth/register",
   SIGN_IN = "/auth/login",
   DETAILS = "/detail",
   AUTHOR = "/author",
   CONFIRMATION = "/auth/confirmation",
   CONFIRMED = "/auth/confirmed",
}
